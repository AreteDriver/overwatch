FROM python:3.12-slim

WORKDIR /app

COPY pyproject.toml .
COPY overwatch/ overwatch/
COPY dashboard/ dashboard/

RUN pip install --no-cache-dir ".[dashboard]"

EXPOSE 8520

CMD ["streamlit", "run", "dashboard/app.py", "--server.port=8520", "--server.address=0.0.0.0", "--server.headless=true"]
