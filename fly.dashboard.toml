app = "overwatch-dashboard"
primary_region = "ord"

[build]
  dockerfile = "Dockerfile.dashboard"

[http_service]
  internal_port = 8520
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 0

[[vm]]
  size = "shared-cpu-1x"
  memory = "512mb"
